## 本地不依赖docker部署：backend+vite-project

# 启动后端：使用 Maven 插件
cd /Users/a1-6/project/project/backend（springboot 跟目录 pom.xml 目录）
mvn spring-boot:run -Dspring-boot.run.arguments="--server.port=9998"

# 启动前端
nginx -t
    看到 /opt/homebrew/etc/nginx/nginx.conf ,最后一行配置include servers/* # 会让nginx去读 基于本文件的位置下 servers/*里面的配置文件

cp /Users/a1-6/project/nginx/test1/angular.conf /opt/homebrew/etc/nginx/servers/
cp /Users/a1-6/project/nginx/test1/nginx.conf /opt/homebrew/etc/nginx/nginx.conf

# 访问 http://localhost:9999/
