## 本地不依赖docker部署：负载均衡+ssl

# 通过go 启动两个后端：
cd /Users/a1-6/project/nginx/test2
go run main.go
go run main2.go

# 启动前端
nginx -t
    看到 /opt/homebrew/etc/nginx/nginx.conf 
cp /Users/a1-6/project/nginx/test1/nginx.conf /opt/homebrew/etc/nginx/nginx.conf

# 访问  https://localhost/app1
    （轮询到 8881/8882）

# 访问  https://localhost/app1 
    固定到 8882）

# 访问 https://localhost
    显示 Nginx 默认欢迎页（html/index.html）
# 访问 http://localhost:80 重定向：会转跳到 https://localhost/
